let variable = 123;
console.log (`= ${variable}`);

variable = variable + 2; // Довгий запис
variable += 2; // Короткий запис, те саме що й довгий тільки легше записується
console.log (`= ${variable}`);

variable = variable - 4; // Довгий запис
variable -= 4; // Короткий запис, те саме що й довгий тільки легше записується
console.log (`= ${variable}`);

variable = variable * 3; // Довгий запис
variable *= 3; // Короткий запис, те саме що й довгий тільки легше записується
console.log (`= ${variable}`);

variable = variable / 5; // Довгий запис
variable /= 5; // Короткий запис, те саме що й довгий тільки легше записується
console.log (`= ${variable}`);

variable = variable % 6; // Довгий запис
variable %= 6; // Короткий запис, те саме що й довгий тільки легше записується
console.log (`= ${variable}`);